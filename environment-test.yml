# Use this environment file when running the tests, when scyjava will be
# installed from source 
name: scyjava
channels:
  - conda-forge
  - defaults
dependencies:
  - pytest
  - pytest-cov
  - jpype1 >= 1.3.0
  - jgo
  - numpy
  - pandas
